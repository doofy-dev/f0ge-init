App(
    appid="rendertest",
    name="Render test",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="render_app",
    cdefines=["APP_RENDERTEST"],
    requires=["gui"],
    stack_size=3 * 1024,
    order=30,
    fap_category="Games",
    fap_icon_assets="assets"
)